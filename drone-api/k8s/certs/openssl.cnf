[ req ]
default_bits       = 2048
prompt             = no
default_md         = sha256
distinguished_name = dn
req_extensions     = req_ext

[ dn ]
O  = UC3M 
CN = api.drone.com

[ req_ext ]
subjectAltName = @alt_names

[ alt_names ]
DNS.1 = api.drone.com
DNS.2 = officer.drone.com
